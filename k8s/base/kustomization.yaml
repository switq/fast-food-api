apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kubernetes.yaml

# Common annotations
commonAnnotations:
  managed-by: kustomize
labels:
  - includeSelectors: true
    pairs:
      app: fast-food-api
      version: v1
